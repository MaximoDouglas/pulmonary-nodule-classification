comands:

# Start mongodb
sudo service mongod start
mongod --dbpath /media/douglas/hd/mongodb1

# Stop mongodb
sudo service mongod stop

# Restart mongodb
sudo service mongod restart

# Open mongodb terminal
mongo

------

restaurando o banco para abrir na máquina pelo robomongo:

mongorestore -d exams /home/lucas/Documentos/HU/PesquisadeForma/BancoAilton/BancoAilton/public


--------
	
mongo export: mongoexport --db test --collection traffic --out traffic.json

------

mongoimport restores a database from a backup taken with mongoexport. Most of the arguments to mongoexport also exist for mongoimport.

In the following example, mongoimport imports the data in the JSON data from the contacts.json file into the collection contacts in the users database.

mongoimport --db users --collection contacts --file contacts.json




